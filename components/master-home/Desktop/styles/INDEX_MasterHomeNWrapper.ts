import styled from "styled-components"

export const INDEX_MasterHomeNWrapper = styled.div`
  background-image: url("/background/background6.svg");
  background-repeat: no-repeat;
  background-size: 100% auto;

  // padding-top: 77px;

  display: grid;
  // gap: 77px;

  & > :not(:nth-child(1)) {
    padding-inline: 56px;
  }
  
`
