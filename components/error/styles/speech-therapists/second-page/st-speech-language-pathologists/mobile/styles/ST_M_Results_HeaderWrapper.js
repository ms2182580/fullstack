import styled from "styled-components"

export const ST_M_Results_HeaderWrapper = styled.div`
  padding: 32px 14px 16px 14px;

  & > :nth-child(1) {
    position: relative;

    & > span {
      display: inline;
      margin-left: calc(20px + 0.625rem);
    }

    & > svg {
      margin-top: auto;
      margin-bottom: auto;
      position: absolute;
      top: 50%;
      bottom: 50%;
    }
  }
  
  
  
  
`
