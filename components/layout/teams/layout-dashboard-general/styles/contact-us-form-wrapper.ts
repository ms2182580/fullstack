import styled from "styled-components"

export const ContactUsFormWrapper = styled.div`
  padding: 40px;

  width: 615px;

  & > form {
    margin-top: 8px;

    display: grid;
    gap: 16px;

    & > label {
      display: grid;
      gap: 4px;
    }
  }
`
