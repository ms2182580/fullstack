import { Layout_Dashboard_General_HeaderWrapper } from "./styles/Layout_Dashboard_General_HeaderWrapper"

/* 
!FH0
- Create the header
  + Make, when the user scroll down, the background of the header be transparent, but when it arraive to the top, have some solid background color
- Create the care_plan layout
- Put on every children of "general" and "care_plan" a "in construction" image or something
- Keep creating everything
- Add static visual test to all the pages (playwright) even if those are flaky test because it will make an image of the results
*/

export const Layout_Dashboard_General_Header = () => {
  return (
    <Layout_Dashboard_General_HeaderWrapper>
      Layout_Dashboard_General_Header
    </Layout_Dashboard_General_HeaderWrapper>
  )
}
