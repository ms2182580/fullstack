import styled from "styled-components"

export const Logo_HeaderWrapper = styled.header`
  width: 100%;
  width: fit-content;

  a {
    user-select: none;

    width: 100%;
    width: fit-content;
  }
`
