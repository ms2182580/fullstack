import styled from "styled-components"

export const ST_D_CardInfoPaymentWrapper = styled.div`
  display: flex;
  white-space: pre;
  margin-right: 24px;

  & > :nth-child(2) {
    flex-grow: 1;
    display: flex;
    flex-wrap: wrap;
  }
`
