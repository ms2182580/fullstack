import React from 'react'
import { STDetail_AboutWrapper } from './styles/STDetail_AboutWrapper'

export const STDetail_About = () => {
    /* 
     !FH This is for the about
     https://github.com/PawanOsman/ChatGPT
     https://api.pawan.krd/chat/gpt?text=Hello&lang=en
  */

    //  const theQueryToFetch = STData.allData !== undefined || `
    //  Write a good presentation of why people want to see a professional like this (write that like the same profesional wirte it): ${
    //    STData.data[0].name.first
    //  } ${STData.data[0].name.last} have a ${
    //    STData.filters[0].CCC_SLP
    //  }, accepts ${new Intl.ListFormat("en", {
    //    style: "long",
    //    type: "conjunction"
    //  }).format(
    //    STData.filters[0].accepts
    //  )}, serve ages of ${new Intl.ListFormat("en", {
    //    style: "long",
    //    type: "conjunction"
    //  }).format(
    //    STData.filters[0].agesServed
    //  )}. Make diagnoses of ${new Intl.ListFormat("en", {
    //    style: "long",
    //    type: "conjunction"
    //  }).format(STData.filters[0].diagnoses)}, is from ${
    //    STData.filters[0].distance
    //  } miles from the current location of the user, speak ${new Intl.ListFormat(
    //    "en",
    //    {
    //      style: "long",
    //      type: "conjunction"
    //    }
    //  ).format(
    //    STData.filters[0].languages
    //  )}, have a meeting format ${new Intl.ListFormat("en", {
    //    style: "long",
    //    type: "conjunction"
    //  }).format(STData.filters[0].meetingFormat)}, is a ${
    //    STData.filters[0].providerType
    //  } provider, have a rating of ${STData.filters[0].rating} with have ${
    //    STData.filters[0].reviews
    //  } reviews, have a service setting of ${new Intl.ListFormat("en", {
    //    style: "long",
    //    type: "conjunction"
    //  }).format(
    //    STData.filters[0].serviceSetting
    //  )}, it allows ${new Intl.ListFormat("en", {
    //    style: "long",
    //    type: "conjunction"
    //  }).format(
    //    STData.filters[0].sessionType
    //  )} session type, have a tranportation of ${new Intl.ListFormat("en", {
    //    style: "long",
    //    type: "conjunction"
    //  }).format(STData.filters[0].transportation)} and have ${
    //    STData.filters[0].yearsOfPractice
    //  } of practice
    //  `
    //  console.log('theQueryToFetch:', theQueryToFetch)
    //  useEffect(() => {
    //    const fetchData = async () => {
    //      const getData = await fetch(
    //        "https://api.pawan.krd/chat/gpt?text=Hello&lang=en"
    //      )
    //    }
    //  })
  
  
  return (
    <STDetail_AboutWrapper>STDetail_About</STDetail_AboutWrapper>
  )
}
